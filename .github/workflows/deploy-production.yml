name: Server Deployment Disabled

on:
  workflow_dispatch:

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment is disabled
        run: |
          echo "Automatic server deployment via SSH is currently disabled."
          echo "Use tag-based GitHub Releases instead (workflow: release.yml)."
